// config.example.js
//
// Copy this file to config.js and fill in your Firebase credentials.
// Without config.js the app runs in DEMO MODE (local-only, no group sync).
// See SETUP.md for step-by-step Firebase setup instructions.
//
// IMPORTANT: config.js is in .gitignore — never commit it to a public repo.

window.FIREBASE_CONFIG = {
  apiKey:            "YOUR_API_KEY",
  authDomain:        "YOUR_PROJECT.firebaseapp.com",
  databaseURL:       "https://YOUR_PROJECT-default-rtdb.europe-west1.firebasedatabase.app",
  projectId:         "YOUR_PROJECT_ID",
  storageBucket:     "YOUR_PROJECT.appspot.com",
  messagingSenderId: "YOUR_SENDER_ID",
  appId:             "YOUR_APP_ID"
};
